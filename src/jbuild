(jbuild_version 1)

(menhir
 ((modules (migemo_dict_parser))))
(menhir
 ((modules (migemo_conv_parser))))
(ocamllex (migemo_dict_lexer migemo_conv_lexer))

(library
 ((name migemocaml)
  (public_name migemocaml)
  (flags (:standard -safe-string))))
